# Invoice AI Reader

Invoice AI Reader is a small full-stack project that allows uploading invoice PDFs and extracting structured data using an AI model.

The main goal is to automatically parse Swiss invoices and return a clean, validated JSON structure.

---

## 🧱 Project structure

```text
Invoice-AI-reader/
├── backend/        # Quarkus REST API
├── frontend/       # React (Vite) frontend
├── README.md
└── .gitignore
